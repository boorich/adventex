{
  "name": "manage_player",
  "description": "Allows the player to view and update their player character's information.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "description": "The action to perform on the player character's information (e.g., view, update)."
      },
      "attribute": {
        "type": "string",
        "description": "The attribute to update (if applicable)."
      },
      "new_value": {
        "type": "string",
        "description": "The new value for the specified attribute (if updating)."
      }
    },
    "required": ["action"]
  }
}
